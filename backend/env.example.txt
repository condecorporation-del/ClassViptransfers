# Database
# For Supabase (RECOMMENDED):
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true&connection_limit=1"

# For local PostgreSQL:
# DATABASE_URL="postgresql://user:password@localhost:5432/los_cabos_luxe?schema=public"

# Server
PORT=3001
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:8080

# Frontend URL (for PayPal return URLs)
FRONTEND_URL=http://localhost:8080
# Backend URL (for confirmation PDF link in emails; default: FRONTEND_URL or http://localhost:3001)
# BACKEND_URL=https://api.yourdomain.com

# PDF download link signing (optional; falls back to JWT_SECRET/ADMIN_JWT_SECRET)
# BOOKING_PDF_SECRET=your_secret_min_32_chars

# Admin (for audit logs - optional)
ADMIN_USER_ID=admin

# PayPal Configuration
# Get these from: https://developer.paypal.com/dashboard/applications/sandbox
# For production: https://developer.paypal.com/dashboard/applications/live
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
PAYPAL_WEBHOOK_ID=your_webhook_id
PAYPAL_ENV=sandbox
# Options: sandbox | live

# Email Configuration (Resend)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key
EMAIL_FROM="Class VIP Transfers <no-reply@classviptransfers.com>"
EMAIL_COMPANY_TO=Armando@caboviptransfers.com
# Add more company emails (comma-separated): email1@example.com,email2@example.com
# Logo (official, high-res): use absolute URL in production for emails/PDF
EMAIL_LOGO_URL=https://res.cloudinary.com/dpmozdkfh/image/upload/v1772074422/Gemini_Generated_Image_zbgk2uzbgk2uzbgk_jnj3n2.png
# Watermark for emails (PNG with ~5% opacity baked in; optional)
# EMAIL_WATERMARK_LOGO_URL=https://your-domain.com/logo-watermark.png

# OpenAI Configuration (AI sales agent)
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo
OPENAI_TEMPERATURE=0.8
OPENAI_MAX_TOKENS=1500
OPENAI_WHISPER_MODEL=whisper-1

# Admin Authentication
ADMIN_EMAIL=admin@caboviptransfers.com
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here
ADMIN_JWT_SECRET=your_jwt_secret_here_min_32_chars
ADMIN_AUTH_DISABLED=false
# Set ADMIN_AUTH_DISABLED=true for development (bypasses auth)
